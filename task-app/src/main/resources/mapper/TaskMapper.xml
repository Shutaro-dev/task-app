<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.task_app.mapper.TaskMapper">
    <resultMap id="taskResultMap" type="com.example.task_app.model.Task">
        <id property="taskId" column="id"/>
        <result property="title" column="title"/>
        <result property="roleId" column="role_id"/>
        <result property="isPermanent" column="is_permanent"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.task_app.model.Task" useGeneratedKeys="true" keyProperty="taskId">
        INSERT INTO tasks (title, role_id, is_permanent, created_at, updated_at)
        VALUES (#{title}, #{roleId}, #{isPermanent}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findAll" resultMap="taskResultMap">
        SELECT id, title, role_id, is_permanent, created_at, updated_at
        FROM tasks
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="taskResultMap">
        SELECT id, title, role_id, is_permanent, created_at, updated_at
        FROM tasks
        WHERE id = #{id}
    </select>

    <select id="findByRoleId" parameterType="java.lang.Integer" resultMap="taskResultMap">
        SELECT id, title, role_id, is_permanent, created_at, updated_at
        FROM tasks
        WHERE role_id = #{roleId}
        ORDER BY id ASC
    </select>

    <update id="update" parameterType="com.example.task_app.model.Task">
        UPDATE tasks
        SET title = #{title}, is_permanent = #{isPermanent}, updated_at = #{updatedAt}
        WHERE id = #{taskId}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM tasks
        WHERE id = #{id}
    </delete>
</mapper> 